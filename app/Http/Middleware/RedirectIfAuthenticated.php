<?php
/*
namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;
use Illuminate\Support\Facades\Auth;

class RedirectIfAuthenticated
{
    /**
     * Handle an incoming request.
     *
     * @param  \Closure(\Illuminate\Http\Request): (\Symfony\Component\HttpFoundation\Response)  $next
     */
/*    public function handle(Request $request, Closure $next, $guard = null)
    {
        if (Auth::guard($guard)->check()) {
            // Si el usuario está autenticado, redirige a otra ruta en lugar de mostrar la página de inicio de sesión
            return redirect('/showUsers'); // Cambia '/dashboard' por la ruta a la que quieres redirigir
        }

        return $next($request);
    }
}
*/

//LO HE CREADO YO, NO ESTA EN USO